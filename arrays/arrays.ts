const heros: string[] = [];
const phone: number[] = [];

// push string in array
heros.push("deadpool");
heros.push("miles");

// push number in array
phone.push(123);
phone.push(456);

// array that stores

const myArray: Array<number> = [];

type User = {
  name: string;
  isActive: boolean;
};

const allUsers: User[] = [];
allUsers.push({ name: "cyrus", isActive: false });

const MLModels: number[][] = [
  [255, 255, 255],
  [255, 255, 255],
];
